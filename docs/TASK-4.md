# Add AI chat but still just Mutual NDA

Now change the way the product interact with a user. Instead of a series of questions, this should be a freeform chat with an AI. The AI asks about the document, asks questions related to the fields, and populates the document based on the responses.