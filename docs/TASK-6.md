# Support multiple users and final polish
 
Add a proper sign up and sign in screen that lets the user register, and come back in to the platform. Store previously generated documents and allow the user to look back at prior documents that they have created (The database can be temporary and be reset every time the server restarts).

Also add polist all the screens so that they look like a professional SaaS application.

Also add a disclaimer that the documents should be considered draft and are subject to legal review.